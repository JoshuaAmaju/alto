import React from 'react';
import {style} from './index.scss';

const Snackbar = ({}) => {

    return (
        <div className={style}>
            
        </div>
    );
};

Snackbar.propTypes = {

};

export default Snackbar;
